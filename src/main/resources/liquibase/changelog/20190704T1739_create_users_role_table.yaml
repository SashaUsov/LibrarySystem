databaseChangeLog:

    - changeSet:
        id: create_users_role_table
        author: sasha_vosu
        changes:

        - createTable:
            tableName: users_role
            columns:
              - column:
                 name: user_id
                 type: bigint
                 constraints:
                    nullable: false
              - column:
                 name: role
                 type: varchar(9)
                 constraints:
                    nullable: false